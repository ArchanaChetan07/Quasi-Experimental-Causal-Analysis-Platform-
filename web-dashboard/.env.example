# No secrets are required for local development — the dashboard reads
# data/results.json directly from disk. This file is a placeholder for
# future config (e.g. if results move behind a real API/database).

# PORT=3000
# NEXT_PUBLIC_ANALYTICS_ID=
